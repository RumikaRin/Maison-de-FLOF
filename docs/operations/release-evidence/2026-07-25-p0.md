# FLOF P0 Release Evidence — 25/07/2026

## Scope

- Environment: Vercel + Neon demo/test
- VNPay: excluded
- Database reset/seed/destructive migration: prohibited

## Evidence

| Gate | Status | Evidence |
|---|---|---|
| Local preflight | PASS | Feature branch and CLI authentication checked without secret output; unrelated `.ai-understand/` remains untouched |
| Local release gate | PASS | PostgreSQL 18 test service healthy; 8 migrations deployed with none pending; fixtures, lint, 100 unit tests, 3 environment-contract tests, build, typecheck, OpenAPI 99/99, production dependency audit, 15 integration tests, 20 Chromium/axe E2E tests (including mobile products), and all 3 Lighthouse URL assertions passed |
| Lighthouse Windows reliability | PASS | Each configured URL runs in an isolated LHCI process; the guarded filesystem shim defers only the known Windows `EPERM` on `TEMP/lighthouse.<id>` after report generation, while unrelated cleanup errors remain fatal |
| PR #2 merged | PASS | Required checks passed on exact head SHA; squash merge completed as `eb62b66` without administrator bypass |
| GitHub CI and main protection | PASS | `quality`, Vercel, and Preview Comments passed; `main` now requires strict `quality` + `Vercel`, linear history, and resolved conversations, with force-push and deletion disabled |
| Vercel production deployment | PASS | Final deployment `dpl_AbNLezfjPij3eiZ92xNBSNtVMGQ9` is `READY`, target SHA matches `origin/main`, all three public aliases serve HTTPS 200, 10/10 smoke routes pass, and the last-hour error/fatal log query returned zero events |
| Neon restore branch | PASS | Temporary `p0-restore-20260725` restored from `production`, aggregate reads succeeded across three tables, and the branch expires automatically after two days |
| Prisma migration status | PASS | Reconcile SQL contained no destructive statement; the one pending migration deployed successfully; final status is up to date and required enum/table/migration metadata exists |
| Cron authorization/execution | PASS | Sensitive 48-byte generated secret is configured for Production and Preview; unauthenticated request returns 401 and an in-memory authorized check returns 200 without retaining the secret |
| Upstash readiness | BLOCKED | Free Singapore resource installation requires explicit acceptance of Vercel/Upstash Marketplace legal terms; no resource or credentials were created |
| Resend acceptance | BLOCKED | Sanitized readiness and domain API checks reject the configured remote credential; no key or mailbox value was printed |
| Cloudinary disposable lifecycle | BLOCKED | Sanitized lifecycle and API ping reject the configured remote credential; no asset remained from the failed check and no credential value was printed |
| Alert delivery | NOT VERIFIED | Vercel default team-wide error/critical rule auto-subscribes owners/admins and deployment failures use web/email by default, but neither Vercel nor Neon exposed a non-destructive test-alert command to prove delivery to the owner |
| Application rollback drill | PASS | Candidate was smoke-tested, production was promoted to known-good `dpl_ETtrbyMRw99QgA7ycx7SwAgbFdCx` and passed 10/10 smoke routes, then restored to `dpl_AbNLezfjPij3eiZ92xNBSNtVMGQ9` and passed 10/10 again |

## Safety Notes

- No secret values, connection strings, tokens, passwords, or authorization headers are retained here.
- Every status remains `NOT VERIFIED` until direct command or runtime evidence exists.
- VNPay-specific behavior is outside this release program and is not included in the completion score.
