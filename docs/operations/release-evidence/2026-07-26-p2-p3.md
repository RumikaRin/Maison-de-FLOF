# FLOF P2/P3 Release Evidence — 26/07/2026

Scope: all P2/P3 work in `docs/superpowers/plans/2026-07-26-flof-p2-p3-completion.md`.
VNPay remains an explicitly excluded simulation. No secret values are recorded.

## Local release gate

| Gate | Result | Evidence |
|---|---|---|
| Test PostgreSQL | PASS | PostgreSQL 18 container healthy; fixtures loaded |
| Prisma migration | PASS | 13 migrations, no pending migration on `flof_test` |
| Lint / typecheck / build | PASS | ESLint exit 0; `tsc --noEmit` exit 0; Next.js production build generated 60 static pages |
| Unit | PASS | 152 passed, 0 failed |
| Coverage | PASS | Global L/F/B 89.48/88.37/89.42%; critical 96.78/100/92.38% |
| Integration | PASS | 24 passed, 0 failed |
| Playwright | PASS | 73 passed across Chromium full suite plus Firefox/WebKit/mobile smoke |
| Accessibility/responsive | PASS | axe/AX, phone/tablet/desktop journeys and accessible retry states included in the 73 tests |
| Local load | PASS | 4/4; p95 58/21/16/18 ms; 0 unexpected responses; 0 server errors |
| Production-safe load | PASS | Fresh deployment: 1 checked warm-up + 9 measured requests/route, 40 GET total; p95 419/176/389/406 ms; 0 unexpected responses; 0 server errors |
| OpenAPI | PASS | Redocly valid; 64 route files and 115/115 operations covered |
| Bundle | PASS | Shared gzip 100.6 KiB (budget 115); all 11 target routes below 210 KiB |
| Lighthouse | PASS | Three runs per URL; median performance 77/79/79 for home/products/login; accessibility 99/100/100; best practices 96; SEO 100; CLS <= 0.00033 |
| Dependency audit | PASS | `npm audit --omit=dev --audit-level=high`: 0 vulnerabilities |

The first full E2E run exposed test coupling in the public-write rate-limit
scenario: an earlier quote lifecycle test consumed the same local IP budget.
`e2e/public-write-abuse.spec.ts` now uses a documentation-only synthetic IP.
The complete 73-test suite then passed, proving the gate is order-independent.

## Neon migration evidence

- Project: FLOF (`icy-forest-83002363`), PostgreSQL 18.
- Production parent branch: `br-curly-shadow-aoa60ewc`.
- Temporary rehearsal branch: `br-still-mud-aom52blp`.
- SQL review found no `DROP`, `TRUNCATE`, `DELETE FROM`, destructive column
  removal, reset, seed command, or `prisma db push`.
- Production initially had 8 applied migrations.
- The five additive migrations from auth-session registry through blog
  categories were applied on the temporary branch with `prisma migrate deploy`.
- Temporary metadata verification: 13 applied migrations, four new tables,
  three target User columns, two Blog category columns and four seeded rooms.
- The same five migrations were then applied to production. Production
  metadata returned the same counts and `prisma migrate status` reported
  up-to-date.
- The temporary rehearsal branch was deleted after verification.

No row-level customer data or connection string was printed or retained.

## P2/P3 acceptance map

| Capability | Source evidence | Runtime/test evidence |
|---|---|---|
| Stable API errors | `src/lib/api-error-contract.ts` | unit source inventory + HTTP error assertions |
| Email ownership | verification auth routes and token helpers | auth lifecycle E2E |
| Revocable sessions | `AuthSession`, `sessionVersion`, profile sessions API | session unit + revocation/demotion E2E |
| Admin TOTP MFA | MFA credential/crypto/TOTP helpers, profile APIs and Security UI | RFC vectors + setup/recovery/login/disable admin MFA E2E |
| Public write protection | middleware policy manifest | policy unit + abuse E2E |
| Production CSP | nonce middleware and CSP-safe UI primitives | CSP source/unit + browser matrix |
| Governed audit | `/admin/audit`, filtered API, sanitizer | integration + audit E2E/axe |
| Privacy lifecycle | export/delete APIs and retention cron | privacy integration + HTTP E2E |
| Visualizer persistence | room/design models, APIs and UI | ownership integration + visualizer E2E |
| Related blog | category migration and ranking service | ranking unit + HTTP |
| Locale routes | locale middleware/navigation helpers | locale unit + browser E2E |
| Notification efficiency | ETag API and bounded polling controller | polling unit + hidden-tab E2E |
| Responsive/accessibility | shared async states and responsive journeys | 73-test browser gate |
| Distributed rate limit | Vercel/direct Upstash environment policy | Vercel Marketplace `sin1` resource Available + sanitized live PING PASS |
| Performance budgets | bundle script + Lighthouse CI config | bundle and 3-run Lighthouse pass |

## External/manual evidence

| Item | Status | Reason |
|---|---|---|
| Google OAuth production | NOT VERIFIED | Requires live provider redirect/consent |
| Resend mailbox delivery | NOT VERIFIED | Unit/provider contract is not mailbox receipt |
| Cloudinary credential lifecycle | NOT VERIFIED | Requires accepted remote credentials |
| Upstash production PING/fail-closed path | PASS | Vercel Marketplace Free `sin1` resource Available; sanitized live PING returned PASS |
| Alert delivery to an on-call owner | NOT VERIFIED | No delivered test alert evidence |
| Manual NVDA/VoiceOver journey | NOT VERIFIED | Automated axe/AX is not a screen reader session |
| Legal approval of retention/anonymization | NOT VERIFIED | No signed policy/PRD/SRS supplied |
| Production load profile | PASS | Fresh production deployment passed 40 canonical GET requests; warm-up status checked separately and measured p95 stayed below 2.5s |
| Production RUM | PARTIAL | Web Analytics and Speed Insights enabled; not enough elapsed traffic for a representative baseline |

## Remote release state

- PR [#6](https://github.com/RumikaRin/Maison-de-FLOF/pull/6) merged through
  branch protection without bypass.
- PR `quality` run `30178933111` passed in 8m46s.
- Preview deployment `dpl_DVVjX3ZbaEhgzopvbeMUedeDmrVf` reached READY for
  exact head SHA `4e9797d43ced79e69f5b6d16f5e8eeb3819b0460`.
- Merge SHA: `35f70260e32a0e74d28aed99d9c9c0aea39920a1`.
- Production deployment `dpl_33urHk6oW12foaUEjcSycmfS6Hji` reached READY
  for the exact merge SHA above.
- Canonical production smoke passed 11/11 routes/status contracts, including
  both protected cron routes returning 401 without credentials.
- Vercel runtime query for HTTP 5xx on the production deployment returned no
  matching logs in the checked 30-minute window.
- Post-merge GitHub run `30179199673` passed the complete gate in 8m49s on the
  exact merge SHA.

## P4 external-readiness release addendum

- PR [#8](https://github.com/RumikaRin/Maison-de-FLOF/pull/8) merged the
  external-readiness implementation after `quality` run `30190018819` passed
  in 8m45s and Vercel preview passed.
- Its first post-deploy load run returned all 40 expected statuses and no 5xx,
  but cold products/colors exceeded the steady-state p95 budget. This evidence
  was kept as a failed gate rather than hidden by an immediate rerun.
- PR [#9](https://github.com/RumikaRin/Maison-de-FLOF/pull/9) corrected the
  runner to use one checked warm-up plus nine measured requests per route,
  preserving exactly 40 GET requests. `quality` run `30190937112` passed in
  6m54s.
- Final merge SHA: `cb490f209acaa4fd60134decca737386929018a6`.
- Post-merge GitHub run `30191140268` passed the complete gate in 8m15s.
- Production deployment `dpl_Eym75ukCNGoKtgyGHPukRyLC7Gcn` reached READY for
  that exact SHA and served all three public aliases.
- The first load-profile run on the fresh deployment passed 40/40 expected
  statuses with p95 products/colors/blog/profile of 419/176/389/406 ms and no
  server errors.
- Canonical smoke passed 11/11 status contracts. A deployment-scoped 20-minute
  query returned zero 5xx log lines.
- Upstash sanitized PING passed from Vercel production variables with local
  `.env` files temporarily isolated and restored afterward; no secret value was
  printed.
